# Backend API Configuration
# URL for the Python backend service that scrapes EU ETS prices
# For local development: http://localhost:5000
# For Docker: http://backend:5000 (internal) or http://localhost:5000 (external)
VITE_BACKEND_API_URL=http://localhost:5000

# OilPriceAPI Configuration (optional fallback)
# Get your free API key at https://oilpriceapi.com
# The API provides real-time EU ETS (EUA) carbon prices updated every 5 minutes
# This is used as a fallback by the Python backend if ICE scraping fails
OILPRICE_API_KEY=your_api_key_here
